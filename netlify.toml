[build]
  publish = "."

# Edge function — secure proxy for Ledger API calls
[[edge_functions]]
  path     = "/.netlify/edge-functions/ledger-api"
  function = "ledger-api"

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"